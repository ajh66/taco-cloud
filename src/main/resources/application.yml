spring:
  profiles:
    active: development
  data:
    rest:
      base-path: /api

---
spring:
  profiles: development
  data:
    cassandra:
      keyspace-name: tacocloud
      schema-action: recreate-drop-unused

taco:
  config:
    page-size: 10
  security:
    sampleUserName: andy
    samplePassword: 1234
    sampleUserRole: USER