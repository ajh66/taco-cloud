spring:
  profiles:
    active: development
  data:
    rest:
      base-path: /api

---
spring:
  profiles: development

taco:
  config:
    page-size: 10
  security:
    sampleUserName: andy
    samplePassword: 1234
    sampleUserRole: USER